server:
  port: 8080

spring:
  datasource:
    username: postgres
    password: tony123
    url: jdbc:postgresql://localhost:5432/tony_hotel_db
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
      file-size-threshold: 2KB